---
title: Organization Overview
---

# Organization Overview

Hierarchical AWS Organization structure with Organizational Units and account hierarchy.

```mermaid
graph TD
    Root["MANAGEMENT<br/>core-root<br/>Billing & Organizations"]
    
    CoreOU["Core OU<br/>Foundation Accounts"]
    PlatOU["Platform OU<br/>Workload Accounts"]
    
    Root --> CoreOU
    Root --> PlatOU
    
    CoreOU --> Audit["core-audit<br/>CloudTrail"]
    CoreOU --> Security["core-security<br/>GuardDuty"]
    CoreOU --> Network["core-network<br/>Transit GW"]
    CoreOU --> DNS["core-dns<br/>Route 53"]
    CoreOU --> Artifacts["core-artifacts<br/>ECR/S3"]
    CoreOU --> Auto["core-auto<br/>CI/CD"]
    
    PlatOU --> Sandbox["plat-sandbox<br/>Experiments"]
    PlatOU --> Dev["plat-dev<br/>Development"]
    PlatOU --> Staging["plat-staging<br/>Pre-prod"]
    PlatOU --> Prod["plat-prod<br/>Production"]
    
    style Root fill:#f90,color:#000
    style CoreOU fill:#e74c3c,color:#fff
    style PlatOU fill:#27ae60,color:#fff
    style Audit fill:#c0392b,color:#fff
    style Security fill:#c0392b,color:#fff
    style Network fill:#3498db,color:#fff
    style DNS fill:#3498db,color:#fff
    style Artifacts fill:#3498db,color:#fff
    style Auto fill:#3498db,color:#fff
    style Sandbox fill:#2ecc71,color:#000
    style Dev fill:#2ecc71,color:#000
    style Staging fill:#3498db,color:#fff
    style Prod fill:#f39c12,color:#000
```

## Key Features

- **Management Account (core-root)**: Organization management and billing consolidation
- **Core OU**: Foundation accounts for shared services (artifacts, audit, auto, dns, network, security)
- **Platform OU**: Workload accounts for different environments (sandbox, dev, staging, prod)
- **Service Control Policies**: Organizational guardrails applied at OU level
- **Account-level isolation**: Each account provides security boundary with cross-account access patterns
